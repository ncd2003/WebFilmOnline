@model ErrorViewModel
@{
    ViewData["Title"] = "Lỗi";
}

<h1 class="text-danger">Lỗi.</h1>
<h2 class="text-danger">Đã xảy ra lỗi khi xử lý yêu cầu của bạn.</h2>

@if (Model?.ShowRequestId ?? false)
{
    <p>
        <strong>Request ID:</strong> <code>@Model?.RequestId</code>
    </p>
}

<h3>Chế độ phát triển</h3>
<p>
    Chuyển đổi sang môi trường <strong>Phát triển</strong> sẽ hiển thị thông tin chi tiết hơn về lỗi xảy ra.
</p>
<p>
    Đối với các ứng dụng đã triển khai, môi trường <strong>Phát triển</strong> không nên được bật vì nó có thể hiển thị thông tin nhạy cảm cho người dùng cuối.
    Đối với gỡ lỗi cục bộ, hãy bật môi trường <strong>Phát triển</strong> bằng cách đặt biến môi trường <strong>ASPNETCORE_ENVIRONMENT</strong> thành <strong>Development</strong>
    và khởi động lại ứng dụng.
</p>
